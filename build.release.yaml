builders:
  proticBuilder:
    import: 'package:blockbyte/builder.dart'
    builder_factories: ['proticBuilder']
    build_extensions: {'.p.html': ['.html']}
    auto_apply: root_package

targets:
  $default:
    builders:
      build_web_compilers|entrypoint:
        options:
          compiler: dart2js
          dart2js_args:
          - --fast-startup
          - --minify
          - --trust-type-annotations
          - --trust-primitives
